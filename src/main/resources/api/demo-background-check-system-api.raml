#%RAML 1.0
title: demo-background-check-system-api

/verify:
  post:
    description: Send Applicant info for Background check
    body:
      application/json:
        example: !include examples/request.json

    responses:
      200:
        body:
          application/json:
            example: !include examples/response.json
